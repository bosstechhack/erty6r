//insert a particular element in array and delete it;
#include<stdio.h>
int main(){
    int s,n;
    scanf("%d",&s);
    int a[s];
    for(int i=0;i<s;i++){
        scanf("%d",a[i]);
    }
    printf("enter the number of element to be delete");
    scnaf("%d",&n);
    for(int i=n-1;i<s;i++){
        for(int j=n;i<s)
     
    }
    return 0;
}